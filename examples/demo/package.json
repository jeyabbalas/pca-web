{
  "name": "pca-web-demo",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Feature-showcase demo for pca-web: workers, progress, abort, WebGPU, IncrementalPCA, model persistence.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit -p tsconfig.json && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "echarts": "^6.1.0",
    "pca-web": "file:../.."
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vite": "^7.3.6"
  }
}
